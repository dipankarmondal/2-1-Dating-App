import { StyleSheet } from "react-native";
import { spacing } from "../../../../utils/helpers/responsive";

export const AccountScreenStyles = StyleSheet.create({
    dt_container: {
        flex: 1,
        padding: spacing.md
    }
})
